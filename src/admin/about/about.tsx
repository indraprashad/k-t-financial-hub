import PageHeader from "@/common/page-header";

const About = () => {
    return (
        <div>
            <PageHeader title='About' subtitle="Manage your about page" />
        </div>
    );
};

export default About;